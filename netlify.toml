[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: client-side routes (/track, /dashboard, /learn/:code) must
# serve index.html instead of 404ing on direct navigation / refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
